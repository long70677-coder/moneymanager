# 開發索引（功能 FUN ↔ 檔案／函式）

> 每次對話開場會自動顯示，方便直接「指路」省 token。
> 結構沿用 `funcList.md` 的 FUN 階層（第0層 FUN2.0銀存 → 第1層 → 第2層）。
> 用法：跟 Claude 講「改 FUN2.1.1 的 FixedWidthParser.ParseRange」或「改 Balances.razor 的 Query()」。
> 改程式後請順手更新本檔；未列出的 FUN＝尚未開發，完整清單見 `funcList.md`。

---

## FUN2.1 暫收

### FUN2.1.1 存摺餘額轉檔　[評估:高]　✅ 已實作
- `CashManagement/Components/Pages/Balances.razor`（`/balances`）— 主畫面
  - `Query()` 查詢、`Upload()` 轉檔、`Preview()` 試轉、`Review()` 全批覆核、`Save()` 人工調整
  - 欄序：編輯/覆核/批號/帳號/帳號用途/系統帳列餘額/銀行存摺轉入餘額/差額/資料來源
- `CashManagement/Services/IngestService.cs` — 三段式 Parse→Map→Write
  - `IngestFiles()`、`IngestFile()`、`PreviewFile()`、`ResolveProfile()`
- `CashManagement/Services/BalanceService.cs` — `BalanceRow`、`GetRows()`、`SaveManualEdits()`、`ReviewBatch()`
- `CashManagement/Services/Parsing/`
  - `DelimitedParser.cs` — 分隔檔；`ParserRegistry.GetParser()`（引擎註冊表）
  - `FixedWidthParser.cs` — 固定寬度(定長)；`Parse()`/`ParseRange()`/`Slice()`/`SplitLines()`；`EncodingHelper.Resolve()`(UTF-8/BIG5)
  - `MappingHelper.cs` — `ParseDate()`(含緊湊 YYYYMMDD/民國)、`ParseAmount()`、`ToHalfWidth()`、`ApplyMapping()`
  - `ParsedProfile.cs` — 格式設定反序列化形態
- `CashManagement/Domain/Ingest.cs` — `ParserEngines`、`ColumnMap`、`ColumnRef`、`AmountFormat`、`ImportResult`、`PreviewResult`
- 支援設定：**銀行格式設定** `Components/Pages/BankFormats.razor`（`/bank-formats`）+ `Services/BankFormatService.cs`
  - `Submit()`(fixedWidth→by=range；分隔檔→by=index)、`ValidateRange()`、`IsFixedWidth`
- 待辦：定長檔結尾彙總行(trailer)略過

### FUN2.1.2 暫收作業　[評估:高]　🟡 部分
- `CashManagement/Components/Pages/Suspense.razor`（`/suspense`）
- `CashManagement/Services/SuspenseService.cs` — `NextBusinessDay()` 等

---

## FUN2.90 基本資料

### FUN2.90.202 銀行帳號基本資料　[評估:中]　✅ 已實作
- `CashManagement/Components/Pages/MasterAccounts.razor`（`/master`、`/master/accounts`）
- `CashManagement/Services/BankAccountService.cs` — `GetAccountCodeOptions()` 等

### FUN2.90.205 帳號維護人員設定　[評估:中]　✅ 已實作
- `CashManagement/Components/Pages/MasterPermissions.razor`（`/master/permissions`）
- `CashManagement/Services/PermissionService.cs` — `GetAccessibleAccountCodes()`

### FUN2.90.308 外幣匯率維護作業　[評估:高]　✅ 已實作
- `CashManagement/Components/Pages/MasterRates.razor`（`/master/rates`）

### 支援用基本資料（非 funcList 獨立 FUN）
- 幣別 `MasterCurrencies.razor`（`/master/currencies`）、假日 `MasterHolidays.razor`（`/master/holidays`）、使用者 `MasterUsers.razor`（`/master/users`）
- 共用：`Services/MasterDataService.cs`、`Services/Master/MasterMaintenanceService.cs`

---

## 規格文件位置（可直接複製路徑對焦）
- `docs/URS/URS2.4.1 投資款收付流程.md` — 投資款收付 使用者需求
- `docs/URS/URS2.90.202 銀行帳號基本資料規格.md` — 銀行帳號基本資料 使用者需求
- `docs/SD/SD_DB_DESIGN.md` — 資料表設計（DDL／唯一鍵／轉檔規則）
- `docs/SD/SD_MASTER_FRAMEWORK.md` — 基本資料維護框架（泛型 CRUD/篩選/停用/匯出）
- `docs/FUN/FUN2.1.1_轉檔架構設計.md` — 存摺餘額轉檔 架構設計（三段式、column_map、DDL §8）
- `docs/FUN/FUN2.1.1_測試文件.md` — 轉檔測試案例
- `docs/流程圖/收付-事務流程圖、工作流程圖.md` — 收付 事務/工作流程圖
- `CashManagement/ARCHITECTURE.md` — 正式版分層架構、鐵律、SQL Server 切換
- `funcList.md` — 全系統功能清單（FUN 階層）

## 共用基礎
- `CashManagement/Program.cs` — 啟動／DI；已註冊 `CodePagesEncodingProvider`(BIG5)
- `CashManagement/Data/Entities.cs` — EF 實體：`BankFormatProfile`、`PassbookBalance`、`LedgerBalance`、`BankAccount`、`AccountManager`…
- `CashManagement/Data/AppDbContext.cs`、`Data/DbSeeder.cs`(demo 資料)
- 啟動：`dotnet run --launch-profile http`（http://localhost:5050）；hot reload 用 `dotnet watch run`

## 全系統待辦（尚未開發，詳見 funcList.md）
- FUN2.2 通報、FUN2.3 調度、FUN2.4 收付、FUN2.5 比對、FUN2.7 結帳、FUN2.8 核銷、FUN2.9 支存、FUN2.10 傳票、FUN2.11 報表、FUN2.80 排程
- FUN2.1.1 內：台幣／外幣分兩 sheet（外幣多幣別呈現待定）
