{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "$p = Join-Path $env:CLAUDE_PROJECT_DIR '開發索引.md'; if (Test-Path $p) { [pscustomobject]@{ systemMessage = [string](Get-Content -Raw -Encoding UTF8 $p) } | ConvertTo-Json -Compress }",
            "statusMessage": "載入開發索引…"
          }
        ]
      }
    ]
  }
}
