# FUN2.1.1 存摺餘額轉檔 — 測試文件

> 對應設計 `FUN2.1.1_轉檔架構設計.md`、資料表 `SD_DB_DESIGN.md`。
> 測試範圍：銀行格式設定、檔案上傳轉檔（路由/檢核/寫入/歷程）、餘額維護畫面（兩餘額並陳/人工調整/全批覆核）、試轉預覽、權限。
> 測試方式：透過執行中的應用程式 API（即各畫面按鈕實際呼叫的端點）。實際結果取自 2026-06-10 執行記錄。

---

## 一、測試環境

| 項目 | 內容 |
|------|------|
| 應用 | Next.js（App Router）+ better-sqlite3，`suspense-app` |
| 進入點 | `/bank-formats`、`/balances`、`POST /api/balance-import` 等 |
| 資料庫 | SQLite `data/suspense.db`（demo，含 seed） |
| 操作者（登入替身）| U001 王小明(STAFF)、U002 李美華(STAFF)、U003 陳主管(MANAGER) |

## 二、測試前置資料

| 類別 | 內容 |
|------|------|
| 銀行格式 profile | `012/NTD`、`012/ZZZ`、`013/NTD`（DELIMITED, UTF-8, 逗號, 表頭1行, 欄名對應 餘額日期/帳號/幣別/餘額） |
| 帳號檔名 | 各帳號 `import_file_name = 帳號短碼.csv`（一檔一帳號） |
| 帳號權限 | 王小明：ACT-001/002（主辦）；李美華：ACT-089/102/103（主辦）；主管：全部 |
| 帳列餘額(結帳產出) | 2025-03-03：ACT-001=2,000,000、ACT-002=5,000,000 |
| 測試檔 | `ACT-001.csv`(2025-03-03,ACT-001,NTD,2000000)、`ACT-002.csv`(…,4980000) |
| 檔案格式 | 表頭 `餘額日期,帳號,幣別,餘額`，逗號分隔，UTF-8 |

---

## 三、測試案例

圖例：結果 ✅=通過。

### A. 銀行格式設定（/bank-formats）

| # | 案例 | 步驟 | 預期 | 實際 | 結果 |
|---|------|------|------|------|------|
| TC-01 | 格式列表 | GET /api/bank-formats | 列出既有 profile，column_map 正確解析 | 3 筆：012/NTD、012/ZZZ、013/NTD | ✅ |
| TC-02 | 新增格式 | POST（014/NTD，欄序對應餘額=第3欄，民國年） | 新增成功回 id | 新增成功 id=4 | ✅ |
| TC-03 | 修改格式 | PUT（改名稱） | changes=1 | 已更新 | ✅ |
| TC-04 | 刪除格式 | DELETE?id=4 | changes=1 | 已刪除 | ✅ |

### B. 轉檔：路由與檢核（/balances 轉檔、POST /api/balance-import）

| # | 案例 | 步驟 | 預期 | 實際 | 結果 |
|---|------|------|------|------|------|
| TC-05 | 檔名路由＋首次轉入 | 王小明上傳 ACT-001.csv | 檔名→ACT-001→profile(012/NTD)→寫入 seq=1 | SUCCESS，import_seq=1、data_type=FILE_IMPORT | ✅ |
| TC-06 | 多檔逐檔獨立 | 同時上傳 ACT-001.csv＋ACT-002.csv | 兩檔各自路由、皆成功 | 成功 2、失敗 0 | ✅ |
| TC-07 | 同批重複檔名 | 上傳兩個同名 ACT-001.csv | 擋整批，回 400 | 400「上傳檔案有重複檔名：ACT-001.csv」 | ✅ |
| TC-08 | 檔名比對不到 | 上傳 UNKNOWN.csv | FAILED，訊息提示設定/手動 | FAILED「檔名比對不到帳號…」 | ✅ |
| TC-09 | 無維護權限 | 李美華上傳 ACT-001.csv（非其負責） | FAILED 權限 | FAILED「您沒有此帳號的維護權限」 | ✅ |
| TC-10 | 更正覆蓋（已立暫收則擋） | 對已立暫收帳號不勾二次重傳 | 擋下，要求先取消立暫收 | FAILED「該帳號餘額已被立暫收，須先取消…」 | ✅ |
| TC-11 | 二次轉入 | 勾「二次轉入」重傳 | import_seq+1，保留前筆 | seq=1 與 seq=2 並存 | ✅ |
| TC-12 | 部分成功 | 檔內含一筆壞金額 | 好的轉入、壞列回報 | 解析1筆、錯誤1筆（金額 BAD） | ✅ |
| TC-13 | 日期解析 | 西元 YYYY-MM-DD | 正確解析、不誤判民國年 | 2024-06-03 正確（修正前曾誤判，已修） | ✅ |
| TC-14 | 檔內日期≠畫面日期 | 檔內日期與餘額日期不符 | 該列擋下並回報 | 回報「檔內日期…與畫面餘額日期…不一致」 | ✅ |

### C. 餘額維護畫面（/balances）

| # | 案例 | 步驟 | 預期 | 實際 | 結果 |
|---|------|------|------|------|------|
| TC-15 | 兩餘額並陳＋差額 | 王小明查 2025-03-03 NTD | 帳列(唯讀)＋存摺＋差額 | ACT-001 差額0；ACT-002 帳列5,000,000 vs 存摺4,980,000＝差額-20,000 | ✅ |
| TC-16 | 人工調整→MANUAL | 勾 ACT-002 改 5,000,000 儲存 | 更新存摺、data_type→MANUAL、回未覆核 | 已儲存、來源MANUAL、差額0 | ✅ |
| TC-17 | 全批覆核 | 按全批覆核 | 該日該幣別範圍覆核、記錄人/時間 | 覆核 2 筆、狀態變已覆核 | ✅ |
| TC-18 | 權限隔離 | 李美華查同條件 | 只見自己帳號 | 只見 ACT-089，看不到 ACT-001/002 | ✅ |
| TC-19 | 轉檔前顯示 | 未轉檔時查詢 | 帳號仍列出、存摺顯示「未轉入」、帳列照帶 | ACT-001/002 帳列有值、存摺未轉入 | ✅ |

### D. 試轉預覽（dry-run）

| # | 案例 | 步驟 | 預期 | 實際 | 結果 |
|---|------|------|------|------|------|
| TC-20 | 解析不寫入 | 對含壞列的樣本檔按試轉預覽 | 回前N列解析結果＋錯誤，不寫DB | 解析1列、錯誤1列（金額BAD），DB 未變動 | ✅ |

---

## 四、端到端流程驗證（Demo 走查）

| 步驟 | 操作 | 觀察結果 |
|------|------|----------|
| 1 | 檢視銀行格式設定 | 3 筆 profile 就緒 |
| 2 | 王小明查 2025-03-03 NTD（轉檔前）| 僅見 ACT-001/002，帳列有值、存摺未轉入 |
| 3 | 上傳 ACT-001.csv＋ACT-002.csv | 各自路由，2 檔皆 SUCCESS |
| 4 | 再查詢 | ACT-001 差額0；ACT-002 差額-20,000 |
| 5 | 勾 ACT-002 改 5,000,000 儲存 | 來源轉 MANUAL、差額歸0 |
| 6 | 全批覆核 | 2 筆覆核完成 |
| 7 | 切李美華查同條件 | 僅見 ACT-089（權限隔離） |

---

## 五、測試結論

- 對應 7 項議題決議（內建設定驅動 parser／一檔一帳號＋ZZZ／轉入次別／全批覆核／兩餘額並陳／逐檔部分成功／同功能參數化）之可驗證項目，**全數通過**。
- 共 20 個測試案例 + 1 條端到端走查，結果皆 ✅。

## 六、已知限制 / 未涵蓋（待 Phase 3）

| 項目 | 說明 |
|------|------|
| 解析引擎 | 僅 `DELIMITED`；`FIXED_WIDTH`／`EXCEL` 未實作 |
| 編碼 | 僅 UTF-8；Big5 需 iconv-lite |
| 帳列餘額來源 | `ledger_balances` 目前以 seed 模擬；正式須由結帳/日結流程供應 |
| 登入 | 以「操作者切換器」替身，未接真實登入 |
| 立暫收取值連動 | 「下游立暫收取最新次別」屬 FUN2.1.2 調整，未納入本次驗證 |

---

_測試日期：2026-06-10｜對應程式：`suspense-app`（commit ba7a15a 起）｜測試者：Claude + 專案負責人_
